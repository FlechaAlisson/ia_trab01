function buscaGulosa(estadoInicial, cores) {
	coresPossiveis = cores;
	forEach(estadoVizinho in estadoInicial){
		coresPossiveis -= estadoVizinho.cor;		
		estadoInicial.cor = random(coresPossiveis);
		buscaGulosa(estadoVizinho, cores);
	}
}